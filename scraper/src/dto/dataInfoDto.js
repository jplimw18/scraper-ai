class DataInfo {
    constructor(mark, model, cpu, mem, storage) {
        this.mark = mark;
        this.model = model;
        this.cpu = cpu;
        this.storage = storage;
        this.mem = mem;
    }
}

module.exports = {
    DataInfo
};