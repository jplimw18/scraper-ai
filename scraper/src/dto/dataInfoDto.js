class DataInfo {
    constructor(mark, model, cpu, mem, storage, price) {
        this.mark = mark;
        this.model = model;
        this.cpu = cpu;
        this.storage = storage;
        this.mem = mem;
        this.price = price;
    }
}

module.exports = {
    DataInfo
};